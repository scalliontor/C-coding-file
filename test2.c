#include<stdio.h>
#include<ctype.h>
#include<math.h>
#include <string.h>
#include <stdlib.h>

int main(){
    int t;
    scanf("%d\n",&t);
    getchar();
    while(t--){
        char a[200];
        gets(a);
        int n = strlen(a);
        int kq[10000];
        for (int i = 0; i < n ; i++){
            kq[]
        }
    }
}